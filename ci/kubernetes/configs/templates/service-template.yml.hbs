apiVersion: v1
kind: Service
metadata:
  name: {{AppName}}
  namespace: {{Namespace}}
  labels:
    app: {{AppName}}
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
  selector:
    app: {{AppName}}